# API base URL (for auth and API calls). Must match API server (e.g. http://localhost:4000).
# CORS on the API uses APP_URL; ensure the browser origin matches APP_URL so cookies are sent.
NUXT_PUBLIC_API_URL=http://localhost:4000
